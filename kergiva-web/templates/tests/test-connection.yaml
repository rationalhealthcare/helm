apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kergiva.fullname" . }}-test-connection"
  labels:
{{ include "kergiva.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kergiva.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
